const SectionHeader = ({ title }: { title: string }) => {
  return <div className="font-extrabold uppercase lg:text-4xl text-3xl -translate-y-10 lg:text-center text-left px-6 -mb-10 lg:mb-0">{title}</div>;
};

export default SectionHeader;
